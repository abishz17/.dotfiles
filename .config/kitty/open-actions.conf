# Open any file with a fragment (line number) in vim
text/plain* action launch --type=overlay --cwd=current vim +${FRAGMENT} -- ${FILE_PATH}
# Open text files without fragments in the editor of your choice
text/* action launch --type=overlay --cwd=current -- ${EDITOR:-vim} -- ${FILE_PATH}
